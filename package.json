{
  "name": "vexon-store",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/sync-catalog.mjs && node scripts/check.mjs && node scripts/build.mjs"
  },
  "engines": {"node": ">=22.18.0"}
}
